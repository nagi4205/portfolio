'use client';

import React from 'react';
import { DrawerModalButton } from './DrawerModalButton';

const DrawerToggleButton = () => {
  return (
    <div>
      <DrawerModalButton />
    </div>
  );
};

export default DrawerToggleButton;
